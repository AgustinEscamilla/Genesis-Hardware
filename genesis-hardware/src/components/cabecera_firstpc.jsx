import { Link } from 'react-router-dom'

export function CabeceraFirstpc({ carrito, al_abrir_carrito = () => {}, acciones_extra = null, autenticado = false, mostrar_carrito = true }) {
  const ruta_catalogo = autenticado ? '/clientes' : '/componentes'
  return <header className="sticky top-0 z-30 border-b border-borde bg-fondo/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between gap-5 px-4 py-4"><Link to={ruta_catalogo} className="flex items-center gap-3 text-texto"><span className="grid h-10 w-10 place-items-center rounded-xl bg-panel text-xl text-primario">▣</span><span className="text-xl font-black tracking-tight texto-degradado">GENESIS HARDWARE</span></Link><nav className="hidden items-center gap-7 text-sm font-semibold text-mutado lg:flex"><Link to="/" className="hover:text-texto">Inicio</Link><Link to={ruta_catalogo} className="text-texto">Catalogo</Link><span>Reportes</span></nav><div className="flex items-center gap-3">{mostrar_carrito && <button onClick={al_abrir_carrito} className="relative rounded-full p-2 text-texto" aria-label="Carrito">🛒{carrito > 0 && <span className="absolute -right-1 -top-1 grid h-5 min-w-5 place-items-center rounded-full bg-primario px-1 text-[10px] font-bold text-fondo">{carrito}</span>}</button>}{acciones_extra || <Link to="/" className="rounded-lg bg-degradado-primario px-4 py-2 text-xs font-bold text-fondo">Iniciar sesion</Link>}</div></div></header>
}
