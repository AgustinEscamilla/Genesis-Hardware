import { Link } from 'react-router-dom'
import { obtener_marca_producto } from '../../services/constantes_firstpc'

export function ResumenProductoFirstpc({ producto, cantidad, set_cantidad, al_agregar, autenticado, mensaje }) {
  const agregar = () => autenticado ? al_agregar(producto, cantidad) : null
  return <div className="space-y-5"><div><p className="text-[10px] font-black tracking-widest text-primario">{obtener_marca_producto(producto)} · COMPONENTES</p><h1 className="mt-2 text-3xl font-black leading-tight text-texto">{producto.nombre}</h1><p className="mt-3 text-sm text-primario">★★★★★ <span className="text-mutado">4.0 (1 reseña)</span></p></div><div className="border-y border-borde py-4"><p className="text-[10px] font-black tracking-widest text-mutado">PRECIO GENESIS HARDWARE</p><p className="mt-1 text-3xl font-black text-texto">${Number(producto.precio || 0).toLocaleString('es-MX')}</p><p className="mt-2 text-xs text-mutado">Envío calculado al confirmar tu pedido</p></div><div className="flex gap-2"><div className="flex items-center rounded-full border border-borde"><button onClick={() => set_cantidad(Math.max(1, cantidad - 1))} className="px-3 py-2">−</button><span className="w-8 text-center text-sm">{cantidad}</span><button onClick={() => set_cantidad(cantidad + 1)} className="px-3 py-2">+</button></div><button onClick={agregar} className="flex-1 rounded-full bg-primario px-5 py-3 text-sm font-bold text-fondo">Agregar al carrito</button><Link to="/componentes" className="grid w-12 place-items-center rounded-full border border-borde text-mutado">♡</Link></div>{mensaje && <p role="alert" className="rounded-lg border border-primario bg-primario/10 p-3 text-xs text-primario">{mensaje}</p>}<p className="text-xs text-terciario">✓ Stock disponible {Number(producto.stockVisible || 0)} piezas</p></div>
}
