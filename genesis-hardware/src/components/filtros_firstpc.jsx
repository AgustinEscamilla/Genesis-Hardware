import { categorias_firstpc, marcas_firstpc, obtener_marca_producto } from '../services/constantes_firstpc'

export function FiltrosFirstpc({ productos, busqueda, set_busqueda, categoria, set_categoria, marca, set_marca, orden, set_orden, minimo, set_minimo, maximo, set_maximo, limpiar_filtros }) {
  return <aside className="h-fit space-y-6 rounded-2xl bg-white p-5 text-slate-900 shadow-sm lg:sticky lg:top-24"><label className="relative block"><span className="sr-only">Buscar productos</span><span className="pointer-events-none absolute left-3 top-2.5 text-slate-400">⌕</span><input value={busqueda} onChange={(evento) => set_busqueda(evento.target.value)} placeholder="Procesadores, ASUS, RTX 4090..." className="w-full rounded-xl border border-slate-200 bg-slate-50 py-2.5 pl-9 pr-3 text-sm outline-none ring-emerald-500 focus:ring-2" /></label><div><p className="text-xs font-black tracking-widest text-slate-900">CATEGORÍAS</p><p className="mt-1 text-xs text-slate-400">13 familias del inventario</p><div className="mt-4 space-y-1"><button onClick={() => set_categoria('Todas')} className={`w-full rounded-lg px-3 py-2 text-left text-sm ${categoria === 'Todas' ? 'bg-slate-950 text-white' : 'text-slate-500 hover:bg-slate-100'}`}>◈ Todas</button>{categorias_firstpc.map((item) => <button key={item} onClick={() => set_categoria(item)} className={`w-full rounded-lg px-3 py-2 text-left text-sm ${categoria === item ? 'bg-slate-950 text-white' : 'text-slate-500 hover:bg-slate-100'}`}>◈ {item}</button>)}</div></div><div><p className="text-xs font-black tracking-widest">MARCAS</p><div className="mt-3 space-y-2">{marcas_firstpc.map((item) => <label key={item} className="flex items-center justify-between text-sm text-slate-500"><span className="flex items-center gap-2"><input type="checkbox" checked={marca === item} onChange={() => set_marca(marca === item ? 'Todas' : item)} className="accent-emerald-500" />{item}</span><span className="text-xs text-slate-400">{productos.filter((producto) => obtener_marca_producto(producto) === item).length}</span></label>)}</div><button className="mt-3 text-[10px] font-black tracking-widest text-emerald-600">VER MÁS</button></div><div className="space-y-3"><label className="block text-xs font-black tracking-widest">ORDENAR PRECIOS<select value={orden} onChange={(evento) => set_orden(evento.target.value)} className="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 p-2.5 text-sm font-normal"><option value="relevantes">Más relevantes</option><option value="menor">Precio: Menor a Mayor</option><option value="mayor">Precio: Mayor a Menor</option></select></label><p className="text-xs font-black tracking-widest">PRESUPUESTO MXN</p><div className="grid grid-cols-2 gap-2"><input value={minimo} onChange={(evento) => set_minimo(evento.target.value)} type="number" placeholder="Mínimo" className="w-full rounded-lg border border-slate-200 bg-slate-50 p-2 text-sm" /><input value={maximo} onChange={(evento) => set_maximo(evento.target.value)} type="number" placeholder="Máximo" className="w-full rounded-lg border border-slate-200 bg-slate-50 p-2 text-sm" /></div><button onClick={limpiar_filtros} className="w-full rounded-xl border border-slate-950 py-3 text-xs font-black tracking-widest text-slate-950 hover:bg-slate-950 hover:text-white">LIMPIAR FILTROS</button></div></aside>
}
