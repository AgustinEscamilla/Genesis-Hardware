import { obtener_marca_producto } from '../services/constantes_firstpc'

export function TarjetaProductoFirstpc({ producto, favorito, al_favorito, al_agregar }) {
  return <article className="group flex flex-col overflow-hidden rounded-2xl bg-white p-3 text-slate-900 shadow-sm transition hover:-translate-y-1 hover:shadow-md"><div className="relative flex h-40 items-center justify-center rounded-xl bg-slate-100 p-4"><button onClick={() => al_favorito(producto.id)} className={`absolute right-3 top-3 text-xl ${favorito ? 'text-rose-500' : 'text-slate-400'}`} aria-label="Marcar favorito">{favorito ? '♥' : '♡'}</button>{producto.imagen ? <img src={producto.imagen} alt={producto.nombre} className="h-full w-full object-contain" /> : <span className="text-4xl text-slate-300">▣</span>}</div><div className="flex flex-1 flex-col p-2"><p className="mt-2 text-[10px] font-black tracking-widest text-slate-400">{obtener_marca_producto(producto)}</p><h2 className="mt-1 line-clamp-2 min-h-10 text-sm font-bold">{producto.nombre}</h2><p className="mt-1 text-xs text-slate-400">{producto.categoria || 'Componentes'}</p><p className="mt-3 text-xs text-amber-500">★★★★★ <span className="text-slate-400">(0)</span></p><div className="mt-auto flex items-end justify-between border-t border-slate-100 pt-3"><div><p className="text-[10px] font-bold tracking-widest text-slate-400">PRECIO</p><p className="text-lg font-black">${Number(producto.precio || 0).toLocaleString('es-MX')}</p></div><button onClick={() => al_agregar(producto)} className="grid h-10 w-10 place-items-center rounded-full bg-slate-950 text-xl text-white hover:bg-emerald-500" aria-label={`Agregar ${producto.nombre}`}>+</button></div></div></article>
}
