import { PanelTablasAdministrador } from './panel_tablas_administrador'

// pos esto funciona como la vista principal que manda llamar al contenedor de todas las tablas
export function VistaTablasAdministrador() {
  return <PanelTablasAdministrador />
}